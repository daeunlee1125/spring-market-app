<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.shoply.mapper.BannerMapper">

    <insert id="saveBanner">
        INSERT INTO Banner
            (ban_name, ban_img, ban_size, ban_bg_color, ban_location, ban_banner_link, ban_start_date, ban_end_date)
        VALUES
            (#{ban_name}, #{ban_img}, #{ban_size}, #{ban_bg_color}, #{ban_location}, #{ban_banner_link}, TO_DATE(#{ban_start_date}, 'YYYY-MM-DD HH24:MI:SS'), TO_DATE(#{ban_end_date}, 'YYYY-MM-DD HH24:MI:SS'))
    </insert>

    <update id="statusToZero">
        UPDATE banner SET ban_status=0 WHERE ban_no=#{ban_no}
    </update>

    <update id="statusToOne">
        UPDATE banner SET ban_status=1 WHERE ban_no=#{ban_no}
    </update>

    <select id="getBanners2" resultType="kr.co.shoply.dto.BannerDTO">
        SELECT BAN_LOCATION, BAN_IMG FROM banner
        WHERE ban_location=1 OR ban_location=2
    </select>

    <select id="selectProductBanner3" resultType="String">
        SELECT BAN_IMG FROM banner
        WHERE BAN_LOCATION = 3
    </select>

</mapper>