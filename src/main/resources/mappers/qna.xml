<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.shoply.mapper.QnaMapper">

    <select id="qnaCnt2" resultType="kr.co.shoply.dto.AdInfoDTO">
        SELECT
        count(*) as qnaTotal,
        count(CASE WHEN TRUNC(q_rdate) = TRUNC(SYSDATE) THEN 1 END) as todQnaTotal,
        count(CASE WHEN TRUNC(q_rdate) = TRUNC(SYSDATE - 1) THEN 1 END) as yesQnaTotal
        FROM qna
    </select>

    <select id="selectQna2" resultType="kr.co.shoply.dto.QnaDTO">
        SELECT
        SUBSTR(mem_id, 1, 3) || RPAD('*', LENGTH(mem_id) - 3, '*') AS masked_id,
        q_cate2,
        q_title,
        q_rdate
        FROM qna
        order by q_rdate desc
        OFFSET 0 row FETCH NEXT 4 ROWS ONLY
    </select>

    <select id="selectQnaAll3" resultType="kr.co.shoply.dto.QnaDTO">
        SELECT *
        FROM QNA
        <where>
            <if test="cate1 != null and cate1 != ''">
                AND Q_CATE1 = #{cate1}
            </if>
            <if test="cate2 != null and cate2 != ''">
                AND Q_CATE2 = #{cate2}
            </if>
        </where>
        ORDER BY Q_NO DESC
    </select>

    <!-- 1. 필터링된 전체 QNA 개수를 가져오는 쿼리 -->
    <select id="selectQnaCnt3" resultType="int" parameterType="kr.co.shoply.dto.PageRequestDTO">
        SELECT COUNT(*)
        FROM QNA
        <where>
            <if test="cate1 != null and cate1 != ''">
                AND Q_CATE1 = #{cate1}
            </if>
            <if test="cate2 != null and cate2 != ''">
                AND Q_CATE2 = #{cate2}
            </if>
        </where>
    </select>

    <!-- 2. 필터링 및 페이징이 적용된 QNA 목록을 가져오는 쿼리 (Oracle 기준) -->
    <select id="selectQnaByCate3" resultType="kr.co.shoply.dto.QnaDTO" parameterType="kr.co.shoply.dto.PageRequestDTO">
        SELECT *
        FROM QNA
        <where>
            <if test="cate1 != null and cate1 != ''">
                AND Q_CATE1 = #{cate1}
            </if>
            <if test="cate2 != null and cate2 != ''">
                AND Q_CATE2 = #{cate2}
            </if>
        </where>
        ORDER BY Q_NO DESC
        OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
    </select>

    <select id="selectQna3" resultType="kr.co.shoply.dto.QnaDTO">
        SELECT *
        FROM QNA
        WHERE Q_NO = #{q_no}
    </select>

    <update id="updateQnaReply3" parameterType="kr.co.shoply.dto.QnaAnswerRequestDTO">
        UPDATE QNA
        SET Q_REPLY = #{qNo}
        WHERE Q_NO =#{content}
    </update>

    <delete id="deleteQnaList3" parameterType="java.util.List">
        DELETE FROM QNA
        WHERE Q_NO IN
        <foreach item="q_no" collection="list" open="(" separator="," close=")">
            #{q_no}
        </foreach>
    </delete>

</mapper>